\documentclass[12pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[spanish]{babel}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{array}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{caption}
\usepackage{float}
\usepackage{listings}

\geometry{margin=2.5cm}

\hypersetup{
  colorlinks=true,
  linkcolor=blue!60!black,
  urlcolor=blue!60!black,
}

% --- listings style ---
\definecolor{codebg}{RGB}{248,248,248}
\definecolor{codeframe}{RGB}{200,200,200}
\definecolor{kwcolor}{RGB}{0,0,180}
\definecolor{strcolor}{RGB}{163,21,21}
\definecolor{cmtcolor}{RGB}{106,153,85}
\definecolor{numcolor}{RGB}{9,134,88}

\lstdefinestyle{python}{
  language=Python,
  backgroundcolor=\color{codebg},
  frame=single,
  rulecolor=\color{codeframe},
  basicstyle=\ttfamily\small,
  keywordstyle=\color{kwcolor}\bfseries,
  stringstyle=\color{strcolor},
  commentstyle=\color{cmtcolor}\itshape,
  numberstyle=\tiny\color{gray},
  numbers=left,
  numbersep=8pt,
  breaklines=true,
  breakatwhitespace=true,
  showstringspaces=false,
  tabsize=4,
  captionpos=b,
  aboveskip=6pt,
  belowskip=6pt,
}

\lstset{style=python}

\title{\textbf{02 --- Modelado diario}\\
  \large SARIMAX / Prophet / LightGBM / Chronos-2 / Ensembles}
\author{TFM --- Pista diaria}
\date{}

\begin{document}

\maketitle
\tableofcontents
\vspace{1em}

\noindent
Pista \textbf{diaria} del TFM. Resultados producidos por los scripts
\texttt{src/modelos\_diarios.py}, \texttt{src/chronos2\_diario.py} y
\texttt{src/ensemble\_diario\_chronos2.py}; este documento los recoge,
evalúa y compara, en paralelo al notebook mensual.

\medskip
\noindent
\textbf{Diseño de validación:} rolling expanding, 6~ventanas, años de
test 2020--2025. Train acumulativo desde 2015-01-01. Cada modelo predice
los 365~días del año de test y se reporta el MAPE a granularidad diaria,
mensual (suma por mes) y anual (suma por año).

\medskip
\noindent
\textbf{Métrica principal:} MAPE anual, comparable con el baseline mensual
SARIMAX (2{,}92\,\% anual).

% -----------------------------------------------------------------------
\section{Carga de datos}

\begin{lstlisting}[caption={Importaciones y carga de predicciones base}]
from pathlib import Path
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

sns.set_theme(style='whitegrid', context='notebook')
plt.rcParams['figure.dpi'] = 100

ROOT = Path.cwd().parent if Path.cwd().name == 'notebooks' else Path.cwd()
REPORTS = ROOT / 'reports'

# Predicciones de los 7 modelos diarios base (long format)
PRED = pd.read_csv(REPORTS / 'diario_predicciones.csv', parse_dates=['fecha'])
print('Modelos base:', sorted(PRED['modelo'].unique()))
print('Anios test:', sorted(PRED['ano_test'].unique()))
print('Filas:', len(PRED))
PRED.head()
\end{lstlisting}

% -----------------------------------------------------------------------
\section{Modelos evaluados}

Siete modelos compiten en la pista diaria:

\begin{table}[H]
\centering
\caption{Modelos de la pista diaria}
\begin{tabular}{llll}
\toprule
Modelo & Tipo & Exógenas & Notas \\
\midrule
\texttt{naive\_trend}    & benchmark         & ---                              & lineal sobre tendencia anual \\
\texttt{naive\_semanal}  & benchmark         & ---                              & repite la semana del año previo \\
\texttt{sarimax\_d}      & clásico           & HDD18, CDD22, is\_holiday        & SARIMAX $(p,1,q)(P,1,Q)_7$ \\
\texttt{prophet}         & descomposición    & HDD18, CDD22, is\_holiday        & doble estacionalidad nativa \\
\texttt{lightgbm}        & ML tabular        & lags + calendario + temp         & features de lag 1--14, 365 \\
\texttt{tbats\_proper}   & doble estacional  & ---                              & BATS con $T=(7,\,365{,}25)$ \\
\texttt{ensemble}        & combinación       & ---                              & media simple naive\_trend + Prophet + LightGBM \\
\bottomrule
\end{tabular}
\end{table}

\noindent
A esto se añade el \textbf{Chronos-2 zero-shot} y el \textbf{ensemble
50/50} que cierra la pista (véanse secciones~\ref{sec:chronos}
y~\ref{sec:ensemble50}).

% -----------------------------------------------------------------------
\section{Función de métricas y cálculo por iteración}

\begin{lstlisting}[caption={Función MAPE y cálculo de métricas por modelo y ventana}]
def mape(y, yhat):
    y, yhat = np.asarray(y), np.asarray(yhat)
    mask = y != 0
    return float(np.mean(np.abs((y[mask] - yhat[mask]) / y[mask])) * 100)

def metricas_por_iter(df_pred):
    rows = []
    for (modelo, ano), g in df_pred.groupby(['modelo', 'ano_test']):
        # diario
        mape_d = mape(g['real'], g['pred'])
        # mensual
        g_m = g.set_index('fecha').resample('MS').agg(
            real=('real', 'sum'), pred=('pred', 'sum'))
        mape_m = mape(g_m['real'], g_m['pred'])
        # anual
        mape_a = mape([g['real'].sum()], [g['pred'].sum()])
        rows.append({'modelo': modelo, 'ano_test': ano,
                     'MAPE_diario': mape_d,
                     'MAPE_mensual': mape_m,
                     'MAPE_anual': mape_a})
    return pd.DataFrame(rows)

metricas = metricas_por_iter(PRED)
metricas.round(2).head(10)
\end{lstlisting}

% -----------------------------------------------------------------------
\section{Resumen por modelo (media de las 6 iteraciones)}
\label{sec:resumen}

\begin{lstlisting}[caption={Resumen agregado por modelo}]
resumen = (
    metricas.groupby('modelo')[['MAPE_diario','MAPE_mensual','MAPE_anual']]
        .agg(['mean','std'])
        .round(3)
)
resumen.columns = ['_'.join(c) for c in resumen.columns]
resumen.sort_values('MAPE_anual_mean')
\end{lstlisting}

\begin{table}[H]
\centering
\caption{MAPE medio y desviación estándar por modelo (6 ventanas rolling)}
\begin{tabular}{lrrrrrr}
\toprule
Modelo
  & \multicolumn{2}{c}{MAPE diario (\%)}
  & \multicolumn{2}{c}{MAPE mensual (\%)}
  & \multicolumn{2}{c}{MAPE anual (\%)} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}\cmidrule(lr){6-7}
  & media & std & media & std & media & std \\
\midrule
\texttt{ensemble}       & 3{,}886 & 1{,}432 & 2{,}956 & 1{,}719 & 2{,}424 & 1{,}854 \\
\texttt{naive\_semanal} & 5{,}892 & 1{,}094 & 3{,}967 & 1{,}569 & 2{,}754 & 1{,}876 \\
\texttt{prophet}        & 4{,}030 & 1{,}191 & 3{,}366 & 1{,}345 & 3{,}059 & 1{,}496 \\
\texttt{lightgbm}       & 4{,}405 & 1{,}557 & 3{,}705 & 1{,}873 & 3{,}266 & 2{,}108 \\
\texttt{naive\_trend}   & 6{,}340 & 0{,}896 & 4{,}621 & 1{,}400 & 4{,}106 & 1{,}638 \\
\texttt{sarimax\_d}     & 6{,}622 & 1{,}387 & 5{,}499 & 1{,}618 & 4{,}969 & 1{,}766 \\
\texttt{tbats\_proper}  & 9{,}792 & 3{,}438 & 9{,}234 & 3{,}779 & 8{,}181 & 5{,}241 \\
\bottomrule
\end{tabular}
\end{table}

\begin{lstlisting}[caption={Referencia: SARIMAX mensual baseline}]
ref_mensual = pd.DataFrame({
    'MAPE_mensual_mean': [3.65, np.nan],
    'MAPE_mensual_std':  [1.66, np.nan],
    'MAPE_anual_mean':   [2.92, 1.96],
    'MAPE_anual_std':    [1.97, np.nan],
}, index=['SARIMAX mensual (rolling 6 iter)',
          'SARIMAX mensual (hold-out 2024-25)'])
ref_mensual.round(2)
\end{lstlisting}

\begin{table}[H]
\centering
\caption{Referencia: SARIMAX mensual}
\begin{tabular}{lrrrr}
\toprule
& MAPE mensual & std mensual & MAPE anual & std anual \\
\midrule
SARIMAX mensual (rolling 6 iter)     & 3{,}65 & 1{,}66 & 2{,}92 & 1{,}97 \\
SARIMAX mensual (hold-out 2024--25)  & ---    & ---    & 1{,}96 & ---    \\
\bottomrule
\end{tabular}
\end{table}

% -----------------------------------------------------------------------
\section{CV walk-forward: detalle por año}
\label{sec:cv}

\begin{lstlisting}[caption={Pivot de MAPE mensual y anual por modelo y año}]
# Pivot: filas = modelo, columnas = ano_test, valores = MAPE_mensual
piv_m = metricas.pivot(index='modelo', columns='ano_test',
                        values='MAPE_mensual').round(2)
piv_m['media']    = piv_m.mean(axis=1).round(2)
piv_m['sin_2020'] = piv_m.drop(columns=[2020, 'media']).mean(axis=1).round(2)
piv_m.sort_values('media')

# Mismo pivot a granularidad anual
piv_a = metricas.pivot(index='modelo', columns='ano_test',
                        values='MAPE_anual').round(2)
piv_a['media']    = piv_a.mean(axis=1).round(2)
piv_a['sin_2020'] = piv_a.drop(columns=[2020, 'media']).mean(axis=1).round(2)
piv_a.sort_values('media')
\end{lstlisting}

\subsection*{MAPE mensual (\%) por modelo y año de test}

\begin{table}[H]
\centering
\caption{MAPE mensual (\%) por modelo y año de test (ordenado por media)}
\begin{tabular}{lrrrrrrrr}
\toprule
Modelo & 2020 & 2021 & 2022 & 2023 & 2024 & 2025 & media & sin 2020 \\
\midrule
\texttt{ensemble}       & 6{,}17 & 2{,}02 &  3{,}63 &  2{,}22 & 1{,}60 & 2{,}09 & 2{,}96 & 2{,}31 \\
\texttt{prophet}        & 5{,}69 & 3{,}66 &  3{,}64 &  2{,}62 & 2{,}86 & 1{,}72 & 3{,}36 & 2{,}90 \\
\texttt{lightgbm}       & 6{,}69 & 2{,}81 &  4{,}82 &  4{,}06 & 2{,}18 & 1{,}67 & 3{,}70 & 3{,}11 \\
\texttt{naive\_semanal} & 6{,}92 & 3{,}71 &  3{,}56 &  4{,}01 & 2{,}22 & 3{,}39 & 3{,}97 & 3{,}38 \\
\texttt{naive\_trend}   & 6{,}51 & 5{,}23 &  3{,}46 &  5{,}73 & 3{,}82 & 2{,}99 & 4{,}62 & 4{,}25 \\
\texttt{sarimax\_d}     & 6{,}35 & 4{,}71 &  4{,}44 &  7{,}86 & 3{,}38 & 6{,}25 & 5{,}50 & 5{,}33 \\
\texttt{tbats\_proper}  & 8{,}58 & 7{,}65 & 14{,}71 & 12{,}47 & 4{,}12 & 7{,}87 & 9{,}23 & 9{,}36 \\
\bottomrule
\end{tabular}
\end{table}

\subsection*{MAPE anual (\%) por modelo y año de test}

\begin{table}[H]
\centering
\caption{MAPE anual (\%) por modelo y año de test (ordenado por media)}
\begin{tabular}{lrrrrrrrr}
\toprule
Modelo & 2020 & 2021 & 2022 & 2023 & 2024 & 2025 & media & sin 2020 \\
\midrule
\texttt{ensemble}       & 5{,}66 & 1{,}99 &  3{,}52 &  1{,}51 & 0{,}79 & 1{,}08 & 2{,}42 & 1{,}78 \\
\texttt{naive\_semanal} & 6{,}13 & 2{,}80 &  2{,}40 &  1{,}53 & 0{,}64 & 3{,}03 & 2{,}76 & 2{,}08 \\
\texttt{prophet}        & 5{,}09 & 3{,}68 &  3{,}57 &  2{,}54 & 2{,}88 & 0{,}59 & 3{,}06 & 2{,}65 \\
\texttt{lightgbm}       & 6{,}24 & 2{,}82 &  4{,}73 &  3{,}78 & 1{,}58 & 0{,}45 & 3{,}27 & 2{,}67 \\
\texttt{naive\_trend}   & 5{,}65 & 5{,}10 &  2{,}25 &  5{,}77 & 3{,}66 & 2{,}21 & 4{,}11 & 3{,}80 \\
\texttt{sarimax\_d}     & 3{,}50 & 4{,}80 &  4{,}16 &  7{,}94 & 3{,}33 & 6{,}08 & 4{,}97 & 5{,}26 \\
\texttt{tbats\_proper}  & 6{,}70 & 7{,}36 & 15{,}04 & 12{,}69 & 0{,}05 & 7{,}25 & 8{,}18 & 8{,}48 \\
\bottomrule
\end{tabular}
\end{table}

% -----------------------------------------------------------------------
\section{Visualización: MAPE por modelo y año}

Boxplot agregando las 6 ventanas, ordenado por mediana.

\begin{lstlisting}[caption={Boxplots de MAPE mensual y anual por modelo}]
orden = (metricas.groupby('modelo')['MAPE_mensual']
                 .median().sort_values().index.tolist())

fig, axes = plt.subplots(1, 2, figsize=(15, 4.5))
sns.boxplot(data=metricas, x='modelo', y='MAPE_mensual', order=orden,
            ax=axes[0], color='lightsteelblue')
axes[0].set_title('MAPE mensual por modelo (6 iter)')
axes[0].tick_params(axis='x', rotation=30)
axes[0].axhline(3.65, color='red', linestyle='--', alpha=0.6,
                label='SARIMAX mensual (3.65 %)')
axes[0].legend()

sns.boxplot(data=metricas, x='modelo', y='MAPE_anual', order=orden,
            ax=axes[1], color='lightsteelblue')
axes[1].set_title('MAPE anual por modelo (6 iter)')
axes[1].tick_params(axis='x', rotation=30)
axes[1].axhline(2.92, color='red', linestyle='--', alpha=0.6,
                label='SARIMAX mensual (2.92 %)')
axes[1].legend()
plt.tight_layout()
\end{lstlisting}

\begin{lstlisting}[caption={Heatmap de MAPE mensual por modelo y año}]
fig, ax = plt.subplots(figsize=(11, 4.5))
sns.heatmap(piv_m.drop(columns=['media','sin_2020']).loc[orden],
            annot=True, fmt='.2f', cmap='RdYlGn_r',
            center=4, ax=ax, cbar=False)
ax.set_title('MAPE mensual (%) por modelo y anio de test')
plt.tight_layout()
\end{lstlisting}

% -----------------------------------------------------------------------
\section{Chronos-2 zero-shot diario}
\label{sec:chronos}

Modelo fundacional \texttt{amazon/chronos-2} (120\,M parámetros,
encoder-only) con \texttt{HDD18}, \texttt{CDD22} e \texttt{is\_holiday}
como \emph{future covariates}. Mismas 6~ventanas. Cuantil mediano
($q=0{,}5$).

\begin{lstlisting}[caption={Carga y evaluación de Chronos-2}]
CHRONOS = pd.read_csv(REPORTS / 'chronos2_diario_predicciones.csv',
                      parse_dates=['fecha'])
m_chronos = metricas_por_iter(CHRONOS).set_index('ano_test')
m_chronos[['MAPE_diario','MAPE_mensual','MAPE_anual']].round(2)

# Resumen global Chronos-2
res_chronos = (m_chronos[['MAPE_diario','MAPE_mensual','MAPE_anual']]
               .mean().to_frame('Chronos-2').T)
res_chronos.round(3)
\end{lstlisting}

\begin{table}[H]
\centering
\caption{MAPE de Chronos-2 zero-shot por año de test}
\begin{tabular}{lrrr}
\toprule
Año test & MAPE diario (\%) & MAPE mensual (\%) & MAPE anual (\%) \\
\midrule
2020 & 6{,}54 & 6{,}02 & 5{,}48 \\
2021 & 2{,}29 & 1{,}72 & 1{,}48 \\
2022 & 4{,}85 & 4{,}59 & 4{,}51 \\
2023 & 2{,}99 & 1{,}80 & 1{,}37 \\
2024 & 2{,}34 & 0{,}99 & 0{,}73 \\
2025 & 3{,}07 & 2{,}04 & 0{,}42 \\
\midrule
\textbf{Media} & \textbf{3{,}681} & \textbf{2{,}859} & \textbf{2{,}330} \\
\bottomrule
\end{tabular}
\end{table}

% -----------------------------------------------------------------------
\section{Ensemble final: $0{,}5 \times \text{ensemble} + 0{,}5 \times \text{Chronos-2}$}
\label{sec:ensemble50}

El modelo entregable de la pista diaria. Promedio simple sin tuning entre
el \texttt{ensemble} (naive\_trend + Prophet + LightGBM) y Chronos-2
zero-shot.

\begin{lstlisting}[caption={Carga del ensemble 50/50 y cálculo de métricas}]
ENS = pd.read_csv(REPORTS / 'ensemble_diario_chronos2_predicciones.csv',
                  parse_dates=['fecha'])

# Metricas del ensemble 50/50 por anio
filas = []
for ano, g in ENS.groupby('ano_test'):
    mape_d = mape(g['real'], g['pred_50_50'])
    g_m = g.set_index('fecha').resample('MS').agg(
        real=('real', 'sum'), pred=('pred_50_50', 'sum'))
    mape_m = mape(g_m['real'], g_m['pred'])
    mape_a = mape([g['real'].sum()], [g['pred_50_50'].sum()])
    filas.append({'ano_test': ano,
                  'MAPE_diario': mape_d,
                  'MAPE_mensual': mape_m,
                  'MAPE_anual': mape_a})

m_ens = pd.DataFrame(filas).set_index('ano_test')
m_ens.round(2)
\end{lstlisting}

\begin{table}[H]
\centering
\caption{MAPE del ensemble 50/50 por año de test}
\begin{tabular}{lrrr}
\toprule
Año test & MAPE diario (\%) & MAPE mensual (\%) & MAPE anual (\%) \\
\midrule
2020 & 6{,}55 & 6{,}07 & 5{,}57 \\
2021 & 1{,}99 & 1{,}13 & 0{,}25 \\
2022 & 4{,}36 & 4{,}10 & 4{,}01 \\
2023 & 2{,}86 & 1{,}73 & 1{,}44 \\
2024 & 2{,}40 & 1{,}09 & 0{,}76 \\
2025 & 2{,}86 & 2{,}06 & 0{,}75 \\
\midrule
\textbf{Media} & \textbf{3{,}503} & \textbf{2{,}699} & \textbf{2{,}129} \\
\bottomrule
\end{tabular}
\end{table}

\begin{lstlisting}[caption={Resumen comparativo: ensemble base, Chronos-2 y ensemble 50/50}]
resumen_final = pd.DataFrame({
    'Ensemble base (naive+Prophet+LGBM)':
        metricas.query("modelo == 'ensemble'")[
            ['MAPE_diario','MAPE_mensual','MAPE_anual']].mean(),
    'Chronos-2 zero-shot':
        m_chronos[['MAPE_diario','MAPE_mensual','MAPE_anual']].mean(),
    'Ensemble 50/50 (FINAL)':
        m_ens[['MAPE_diario','MAPE_mensual','MAPE_anual']].mean(),
}).T.round(3)
resumen_final
\end{lstlisting}

% -----------------------------------------------------------------------
\section{Visualización del ganador}

Predicción del ensemble 50/50 frente al real, agregado a mensual, para
las 6~ventanas.

\begin{lstlisting}[caption={Serie mensual: real vs.\ ensemble 50/50}]
ens_mes = (
    ENS.set_index('fecha')
       .groupby('ano_test')[['real','pred_50_50']]
       .resample('MS').sum()
       .reset_index()
)

fig, ax = plt.subplots(figsize=(13, 4.5))
for ano, g in ens_mes.groupby('ano_test'):
    ax.plot(g['fecha'], g['real']/1e6, color='black', linewidth=1)
    ax.plot(g['fecha'], g['pred_50_50']/1e6,
            color='steelblue', linewidth=1, alpha=0.8)
ax.set_title('Ensemble 50/50 diario -> mensual: real (negro) vs pred (azul)')
ax.set_ylabel('TWh/mes')
plt.tight_layout()
\end{lstlisting}

\begin{lstlisting}[caption={Demanda anual real vs.\ predicha + gráfico de barras}]
# Tabla de error anual
anual = (
    ENS.assign(anio=ENS['fecha'].dt.year)
       .groupby('anio').agg(
           real_TWh=('real',     lambda x: x.sum()/1e6),
           pred_TWh=('pred_50_50', lambda x: x.sum()/1e6))
)
anual['error_%'] = (anual['pred_TWh'] - anual['real_TWh']) / anual['real_TWh'] * 100
anual.round(3)

# Grafico de barras
fig, ax = plt.subplots(figsize=(9, 4.5))
x = np.arange(len(anual))
w = 0.4
ax.bar(x - w/2, anual['real_TWh'], width=w, label='Real', color='black')
ax.bar(x + w/2, anual['pred_TWh'], width=w,
       label='Pred (ensemble 50/50)', color='steelblue')
ax.set_xticks(x); ax.set_xticklabels(anual.index)
ax.set_ylabel('TWh/anio')
ax.set_title('Demanda anual: real vs ensemble 50/50 diario')
ax.legend()
plt.tight_layout()
\end{lstlisting}

\subsection*{Demanda anual: real vs.\ ensemble 50/50}

\begin{table}[H]
\centering
\caption{Demanda anual (TWh) real vs.\ predicha por el ensemble 50/50}
\begin{tabular}{lrrr}
\toprule
Año & Real (TWh) & Pred (TWh) & Error (\%) \\
\midrule
2020 & 250{,}051 & 263{,}973 & $+5{,}57$ \\
2021 & 256{,}546 & 255{,}898 & $-0{,}25$ \\
2022 & 250{,}471 & 260{,}518 & $+4{,}01$ \\
2023 & 246{,}665 & 243{,}112 & $-1{,}44$ \\
2024 & 249{,}000 & 250{,}883 & $+0{,}76$ \\
2025 & 256{,}086 & 254{,}169 & $-0{,}75$ \\
\bottomrule
\end{tabular}
\end{table}

% -----------------------------------------------------------------------
\section{Comparativa final: pista diaria vs.\ baseline mensual}

\begin{lstlisting}[caption={Tabla comparativa global de todos los modelos}]
tabla_final = pd.DataFrame({
    'modelo': ['SARIMAX mensual (baseline)',
               'SARIMAX mensual (hold-out 2024-25)',
               'Ensemble base diario',
               'Chronos-2 diario',
               'Ensemble 50/50 diario (FINAL)'],
    'MAPE_mensual_%': [3.65, np.nan, 2.96, 2.86,
                       m_ens['MAPE_mensual'].mean()],
    'MAPE_anual_%':   [2.92, 1.96, 2.42, 2.33,
                       m_ens['MAPE_anual'].mean()],
}).round(2)
tabla_final
\end{lstlisting}

\begin{table}[H]
\centering
\caption{Comparativa global de modelos}
\begin{tabular}{lrr}
\toprule
Modelo & MAPE mensual (\%) & MAPE anual (\%) \\
\midrule
SARIMAX mensual (baseline)             & 3{,}65 & 2{,}92 \\
SARIMAX mensual (hold-out 2024--25)    & ---    & 1{,}96 \\
Ensemble base diario                   & 2{,}96 & 2{,}42 \\
Chronos-2 diario                       & 2{,}86 & 2{,}33 \\
\textbf{Ensemble 50/50 diario (FINAL)} & \textbf{2{,}70} & \textbf{2{,}13} \\
\bottomrule
\end{tabular}
\end{table}

% -----------------------------------------------------------------------
\section{Conclusiones de la pista diaria}

\begin{enumerate}
  \item \textbf{El ensemble 50/50 (ensemble base + Chronos-2) es el modelo
        entregable de la pista diaria.} MAPE anual $\approx 2{,}1$\,\% en
        rolling 6~iter, superando al baseline mensual SARIMAX (2{,}92\,\%)
        y al propio Chronos-2 en solitario (2{,}33\,\%).

  \item \textbf{Naive\_semanal aguanta sorprendentemente bien} como benchmark
        (MAPE anual $\approx 2{,}75$\,\%), lo que señala que la estacionalidad
        semanal es la información dominante en la granularidad diaria.

  \item \textbf{TBATS no es competitivo a esta escala} (MAPE mensual
        $\approx 9$\,\%), probablemente por el coste de ajustar dos
        estacionalidades simultáneas sobre 365~días con poca historia.

  \item \textbf{SARIMAX diario} (con $D=7$) no mejora al SARIMAX mensual
        una vez agregado. El SARIMAX gana en granularidad mensual, donde
        la estacionalidad anual domina; en diario la estacionalidad semanal
        exige modelos más flexibles (LightGBM, Prophet, Chronos-2).

  \item \textbf{2020 es el fold sucio} en todos los modelos; la columna
        \texttt{sin\_2020} aísla el efecto COVID. Se recomienda reportarla
        aparte en la memoria del TFM.

  \item \textbf{El gap mensual $\to$ anual es positivo}: al agregar los
        365~días, los errores diarios se compensan parcialmente. El ensemble
        50/50 baja de $\approx 2{,}7$\,\% mensual a $\approx 2{,}1$\,\%
        anual.
\end{enumerate}

\bigskip
\noindent
$\to$ La pista diaria queda cerrada con el ensemble 50/50 como modelo final.
El siguiente paso natural es la \textbf{pista horaria} (modelos disponibles
en \texttt{reports/horario\_*}) o la redacción de la memoria.

\end{document}
